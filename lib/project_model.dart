class ProjectModel {
  String title;
  String description;
  String image;
  bool isActive;

  ProjectModel({ required this.title, required this.description, required this.image, required this.isActive });
}